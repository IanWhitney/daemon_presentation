<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="row">
  <div class="col-md-1"><%= f.label :first_name %></div>
  <div class="col-md-11"><%= f.text_field :first_name %></div>
</div>
<div class="row">
  <div class="col-md-1"><%= f.label :last_name %></div>
  <div class="col-md-11"><%= f.text_field :last_name %></div>
</div>
<div class="row">
  <div class="col-md-1"><%= f.label :email %></div>
  <div class="col-md-11"><%= f.text_field :email %></div>
</div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
